export function SiteFooter() {
  return (
    <footer className="mt-10 border-t border-black/5 py-8 text-sm text-neutral-500">
      <div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
        <p>LinkShop agrega ofertas e redireciona para parceiros externos.</p>
        <p>Arquitetura consolidada por features, pronta para backend real, auth real e integrações futuras.</p>
      </div>
    </footer>
  );
}
