NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api
# Optional: server-side only override (SSR/route handlers).
# Use this when the frontend server reaches backend through an internal network URL.
BACKEND_INTERNAL_API_BASE_URL=
